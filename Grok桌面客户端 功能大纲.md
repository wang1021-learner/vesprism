# Grok Build 桌面客户端 —— 从 0 到 1 完整功能大纲

> 整理时间：2026-07-23
> 说明：本文档梳理 Grok Build 二次开发桌面端截至目前的全部功能范围，包括已完成、明确不做、以及尚未开始的部分，供后续规划参考。

---

## 一、核心引擎层（Agent Runtime 集成）

| 功能 | 状态 |
|---|---|
| Library 依赖集成（非子进程套壳） | ✅ |
| 内存双工管道通信（`tokio::io::duplex`） | ✅ |
| ACP 协议握手（`initialize` + `new_session`） | ✅ |
| 流式事件转发（文本/思考/工具调用） | ✅ |
| 权限请求异步交互（`oneshot` 挂起等待） | ✅ |
| 取消生成（`cancel`） | ✅ |
| 会话恢复（`load_session`/`resume`） | ✅ |
| 会话重启/新建（`Restart`，复用销毁重建套路） | ✅ |
| 会话删除（含"当前会话"边界处理） | ✅ |
| 空会话判定与自动清理 | ✅ |
| 会话重命名（`update_session_title`） | ✅ |
| 多会话并行（同时开多个独立对话窗口/标签） | ⬜ 未做 |
| 会话分支/Fork（从某一轮对话派生新会话） | ⬜ 未做（协议/官方层可能已支持，未接入） |
| 流式响应中断续传/断线重连 | ⬜ 未做 |

---

## 二、配置与模型管理

| 功能 | 状态 |
|---|---|
| 独立 `GROK_HOME` 配置隔离（与命令行/官方目录分离） | ✅ |
| 官方配置读写复用（`load_effective_config`/`update_config`） | ✅ |
| 工作目录选择（原生对话框） | ✅ |
| API Key 状态展示（只读，不回显明文） | ✅ |
| API Key 写入（`.env`，跟随模型 `env_key`） | ✅ |
| 多模型配置（新增/编辑/删除，精简字段） | ✅ |
| 默认模型切换 | ✅ |
| Header 快捷切模型（设置页之外的第二入口） | ⬜ 明确不做（已讨论，避免维护双入口） |
| 模型高级字段编辑（`context_window`/`system_prompt_label` GUI 化） | ⬜ 明确不做（留给手动改 toml，防止误填） |
| CWD 全局统一持久化（跨会话记住上次工作目录） | ⬜ 未做 |
| 配置导入/导出（备份、迁移到另一台机器） | ⬜ 未做 |
| MCP 服务器配置管理（增删 MCP 连接） | ⬜ 未做 |

---

## 三、界面与交互（桌面壳）

| 功能 | 状态 |
|---|---|
| 浅色主题（Claude/Codex 风格） | ✅ |
| 侧边栏历史列表（真实数据，非假数据） | ✅ |
| 侧边栏时间分组（今天/昨天/前天/按日） | ✅ |
| 侧边栏折叠/展开 | ✅ |
| 搜索弹层（本地标题过滤） | ✅ |
| 搜索接入官方全文索引（`session_search.sqlite`，关键词搜正文而非仅标题） | ⬜ 未做 |
| 会话重命名弹窗 | ✅ |
| 删除会话（菜单+确认） | ✅ |
| Header 显示当前对话标题 | ✅ |
| 消息气泡（用户/AI/思考/系统统一视觉语言） | ✅ |
| 思考过程可折叠展示 | ⬜ 部分做（有卡片展示，折叠交互未确认最终态） |
| Markdown 渲染（代码高亮/表格/引用） | ✅ |
| 工具调用可视化卡片（状态/路径/命令摘要，可展开输出） | ✅ |
| 工具调用 diff 高亮（文件编辑前后对比） | ⬜ 未做 |
| 流式终端 stdout 实时展示（长时间命令的中间输出） | ⬜ 未做 |
| 权限确认弹窗（人类可读文案，非 Debug dump） | ✅ |
| 悬浮输入框（圆角卡片+发送按钮） | ✅ |
| 深色主题（第二套配色供选择） | ⬜ 未做 |
| 附件/图片输入（粘贴截图、拖拽文件） | ⬜ 未做（TUI 侧已有 `Alt+V` 方案，桌面未接入） |
| 快捷键体系（如 Cmd+K 搜索、Cmd+N 新建） | ⬜ 未做 |
| 通知/提醒（生成完成时的系统通知，后台切走时） | ⬜ 未做 |
| 多语言/i18n（目前中文写死） | ⬜ 未做 |
| 无障碍支持（键盘导航、屏幕阅读器） | ⬜ 未做 |

---

## 四、稳定性与工程化

| 功能 | 状态 |
|---|---|
| 浏览器误开引导页（防止裸 WebView 报错） | ✅ |
| Windows 编译兼容性修复（`/dev/stdout` 等） | ✅ |
| workspace 集成规范（依赖统一 `workspace = true`） | ✅ |
| 单元测试（`session_update_to_event` 等纯函数） | ✅ |
| 集成测试（端到端会话流程自动化测试） | ⬜ 未做 |
| 错误上报/崩溃日志收集 | ⬜ 未做（`tauri-plugin-log` 已有，仅本地） |
| 性能：超长对话历史的虚拟滚动/分页加载 | ⬜ 未做（历史消息一次性全量加载，长会话可能卡顿） |
| 安全：密钥存储从明文 `.env` 升级到系统密钥链（Windows Credential Manager/macOS Keychain） | ⬜ 未做 |
| 自动更新机制（Tauri updater） | ⬜ 未做 |
| 安装包/分发（签名、安装向导） | ⬜ 未做 |
| 跨平台验证（目前仅 Windows，macOS/Linux 未测试） | ⬜ 未做 |

---

## 五、协作与扩展能力（远期，非近期规划）

| 功能 | 状态 |
|---|---|
| 会话导出（分享一段对话为 Markdown/PDF） | ⬜ 未做 |
| 团队共享配置（多人共用同一套模型/MCP 配置） | ⬜ 未做 |
| 插件市场接入（官方 `plugin-marketplace` 已在 `config.toml` 里，未做 GUI 管理） | ⬜ 未做 |
| Agent 定义/角色切换（不同 `agent_name` 之间切换，目前写死 `grok-build-plan`） | ⬜ 未做 |

---

## 六、建议的下一阶段优先级

不是所有"⬜"都同等重要，按对当前实际使用的影响排序：

1. **API Key 明文存储升级** —— 安全类问题，此前已因密钥泄露吊销过一次真实密钥，这类风险不该无限期搁置。
2. **超长对话历史性能** —— 目前测试的会话都很短，真正长期使用后这个问题会逐渐暴露，越晚处理排查越难。
3. **搜索接入全文索引** —— 现在只搜标题，历史一多这个限制会很明显。
4. **归档文档同步** —— 一直拖着未做的老账，建议找个时间一次性了结。

其余（多会话并行、深色主题、插件市场、跨平台）更像是"锦上添花"或者"看用户增长再说"，不建议现在分散精力。

---

## 七、贯穿全程的核心原则（供后续协作者参考）

1. **只加不改、不删除、不重构官方代码**，最小化与上游同步的合并冲突成本。
2. **动手写之前，先查官方是否已有对应能力**：查证方式是通读真实源码（函数签名、可见性、依赖链是否可达），而不是凭猜测判断"应该有"或"应该没有"；确认无现成能力后再决定自建，而不是默认自己重新实现一遍。协议细节同理，以真实源码为准，不采信未经验证的猜测性参数/JSON 示例。
   - 案例：会话恢复——查实 `agent-client-protocol` 已有 `load_session`、`xai_grok_shell::agent::MvpAgent` 已完整实现，避免了自造一套读 `chat_history.jsonl` 拼历史的方案（会和官方真实会话状态脱节）。
   - 案例：会话列表读取——查实 `xai_grok_shell::session::persistence::list_summaries` 是现成 `pub` 函数，直接复用，不必自己写目录遍历 + JSON 解析。
   - 案例：配置读写——查实官方有 `update_config`/`save_config`（结构体级）与 `toml_edit::DocumentMut`（保留原文件格式的精确编辑）两套机制，比较后选择更安全的一套，而非直接手写字符串拼接。
3. **敏感配置一律走 `.env` + `.gitignore`**，不硬编码进源代码；密钥一旦可能泄露应立即吊销重建。
4. **跟官方 crate 交互的第三方依赖，一律 `workspace = true`**，不自行指定版本号。
5. **测试/临时环境用独立的 `GROK_HOME` 隔离**，不碰用户真实配置目录。
6. **每一次功能改动都要有独立可验证的步骤**，编译通过不等于逻辑正确，需实际运行验证；涉及数据删除/迁移类改动需额外做背对背代码审查。
7. **系统提示词等"认知层"配置，优先查找官方设计好的覆盖机制**，而非 hack 底层实现。
8. 上游仓库不接受 PR，只能自行 fork 维护，定期 `git fetch upstream` 手动同步。