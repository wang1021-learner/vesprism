[package]
name = "grok-dsh-poc"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
description = "POC: 从 Rust 驱动 DeepSeek Harness SDK runtime（stdio JSON-RPC），验证 Vesprism 引擎级结合通道"

[dependencies]
tokio = { workspace = true, features = ["full"] }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
