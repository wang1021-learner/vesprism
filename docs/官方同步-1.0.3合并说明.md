# 官方同步说明：Grok Build 1.0.0 → 1.0.3

> 文档日期：2026-08-14  
> 远程：`upstream` → https://github.com/xai-org/grok-build.git  
> 官方 tip：`eb267fe`（2026-08-12 一带 Synced from monorepo）  
> 合并前本地 tip：`dc3182c`  
> 合并基线：`75e73f3`（本地原先对齐的官方 sync）

---

## 1. 摘要

| 项 | 合并前（本地） | 合并后 |
|----|----------------|--------|
| 官方壳版本 | **1.0.0**（2026-08-07） | **1.0.3**（2026-08-12） |
| `SOURCE_REV` | `a61c32b12a2b400f212221cd8762e05f9b36828d` | `e6a67a5408288c98380cd13f3b1fe1fbc01c9f1f` |
| 相对基线 `75e73f3` | 本地 43 个二次开发提交 | 官方 4 次 monorepo sync |
| 冲突 | — | 4 处（已按「官方 crate 偏官方」解决） |
| 编译验证 | — | `cargo check -p grok-session -p vesprism-desktop` 通过 |
| 桌面 verify | — | `crates/vesprism-desktop/scripts/verify.mjs` |

**策略：** 官方能力整包合入；桌面二次开发（`vesprism-desktop` / `grok-session`）不在上游仓，合并不会被覆盖。胶水层只补官方新字段。

---

## 2. 官方这 4 次 sync

```
eb267fe Synced from monorepo   ← 当前 upstream/main
e5fd481 Synced from monorepo
be71313 Synced from monorepo
b13fa52 Synced from monorepo
75e73f3 Synced from monorepo   ← 合并基线
```

变更量约 **724** 文件。官方 crate 拆出若干独立包（`xai-compaction-transcript`、`xai-fuzzy-file-search`、`xai-grok-active-sessions`、`xai-grok-bundle`、`xai-grok-diag-server`、`xai-grok-foreign-sessions`、`xai-grok-pager-diff`、`xai-grok-workspace-daemon` 等）。

### 2.1 版本摘要（`xai-grok-shell/CHANGELOG.md`）

**1.0.3** — 子 Agent 在大量会话下启动更快；`/session-info` 可复制行。

**1.0.2** — 工具参数流式有独立 spinner；大图会话压缩不再超限；图片被服务端拒绝后可恢复。

**1.0.1（含 breaking）**

- `/rewind` 默认只截断对话、不再改文件，并默认确认。
- Managed MCP 只走 gateway catalog。
- 子 Agent 扇出有队列上限；工具可声明只读；`kill_task` 带 `source`（`clientUi` / `teardown`）。

---

## 3. 冲突怎么解

| 文件 | 倾向 | 处理 |
|------|------|------|
| `Cargo.lock` | 以编译为准 | 先取官方，再 `cargo check` 重生 |
| `xai-grok-shell/Cargo.toml` | 官方 | 收下 `test-util`；保留本地 `playground-tools` |
| `video_gen/mod.rs` | 官方 | 单一模型 `grok-imagine-video-1.5` |
| `xai-tty-utils/src/lib.rs` | 官方 | `attach_std` 文档与 Windows handle 语义 |

桌面专属路径无冲突。

### 3.1 胶水层适配

`KillTaskRequest` 新增必填 `source`。`grok-session::kill_task` 发 `TaskKillSource::ClientUi`（设置页/会话里的 `[×]`），不是会话拆除。

---

## 4. 做功能前对照（官方已有 / 桌面该做）

| 官方 1.0.1–1.0.3 新能力 | 桌面现状 |
|-------------------------|----------|
| rewind 默认只改对话 | 已有 `RewindPicker`；模式含 `conversation_only` / `files_only` / `all`，需对照新默认 |
| `kill_task.source` | 已接 `ClientUi` |
| 工具只读声明 | 引擎侧；桌面权限条可后续用来少弹只读工具 |
| 子 Agent 扇出排队 | 引擎侧；桌面无需改协议 |
| Managed MCP 仅 gateway | `McpPanel` 本地 upsert 仍可用；托管目录以官方为准 |
| 子 Agent 启动加速 | 引擎侧，桌面直接受益 |

---

## 5. 未做的手测

工作流要求的冷启动 / 发消息 / 权限 / 多 Tab 需在本机 Vesprism 里点一遍。本次只做了编译与 `verify.mjs`。
