# 桌面端 UI 主题设计说明

> 日期：2026-07-26  
> 实现文件：`crates/jike-grok-desktop/src/App.css`、`BrandLogo.tsx`  
> 方向：参考 **Apple 官网** 的留白、层次与圆角；**不用纯黑 / 纯白**，以冷白画布 + 天空蓝强调为主。

---

## 1. 设计原则

| 原则 | 做法 |
|------|------|
| 非纯黑白 | 文字用深蓝灰 `#1a2f45`；表面用冷白 `#f7f9fd`；画布用雾蓝 `#e4ecf6` |
| 层次靠色相与投影 | 画布渐变 → 毛玻璃侧栏 → 主内容面板 → 浮层卡片 |
| 主色克制 | 主操作：Apple 蓝 `#0071e3`；辅色：淡紫 `#5b6cff`；发送钮保留暖陶土 CTA 做对比 |
| 圆角与阴影 | 大圆角 18–22px；阴影带蓝调（`rgba(26,80,140,…)`），避免死灰黑影 |
| 字族 | 系统 SF / Segoe，轻微负字距（`-0.01em`） |

---

## 2. 色板（CSS 变量）

定义于 `App.css` 的 `:root`：

| Token | 用途 | 约值 |
|-------|------|------|
| `--bg-canvas` / `--bg-canvas-glow` | 应用背景与光晕 | 雾蓝 / 天蓝 |
| `--surface-elevated` | 卡片、面板 | 冷白 |
| `--surface-muted` / `--surface-hover` | 次级底、hover | 浅蓝灰 |
| `--text-primary` / `--secondary` / `--tertiary` | 正文层级 | 深蓝灰阶 |
| `--accent` | 主按钮、链接、焦点环 | `#0071e3` |
| `--brand` | Logo、思考块、徽章 | `#5b6cff` |
| `--cta` | 发送按钮 | 陶土橙红 |
| `--success` / `--warning` / `--danger` | 语义状态 | 偏柔和 |

改主题时 **只改 `:root` 变量**，组件尽量已走 `var(--*)`。

---

## 3. 主要界面落点

| 区域 | 视觉 |
|------|------|
| 应用背景 | 径向 + 线性雾蓝/淡紫光晕（非平涂灰） |
| 侧栏 | 半透明毛玻璃 + 细蓝边 |
| New chat | Apple 蓝胶囊渐变（替代纯黑条） |
| 会话选中 | 浅蓝软底 `--accent-soft` |
| 用户气泡 | 天空蓝渐变，右下小圆角 |
| 思考块 | 淡紫渐变边 |
| Assistant 徽章 | 蓝紫渐变圆标 |
| Composer | 大圆角卡片；focus 时蓝色光环 |
| 设置导航选中 | 浅蓝底 + 蓝色字（非反白黑条） |
| Logo | 蓝紫渐变底板（非纯黑方块） |

---

## 4. 与旧版差异（摘要）

- 旧：ChatGPT 式极简灰白 + 黑按钮，偏「工具感」。  
- 新：Apple 营销页式 **冷色空气感** + 彩色强调，主操作可辨、背景不死板。

---

## 5. 后续可继续打磨（未做）

- 深色主题（若以后需要，可加 `data-theme="dark"` 第二套变量）  
- 动效：侧栏切换、消息入场（注意 `prefers-reduced-motion`）  
- 独立 `theme.css` 拆分（当前仍集中在 `App.css`）  
- 高对比无障碍档（加大字重/边框）

---

## 6. 如何预览

```bash
cd crates/jike-grok-desktop
cargo tauri dev
```

备份：`src/App.css.bak-before-theme`（若不需要可删）。
