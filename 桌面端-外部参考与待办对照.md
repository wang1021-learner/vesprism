# 桌面端：外部项目对照、可参考点与待办清单

> 整理日期：2026-07-26  
> 用途：后续排期与实现时查阅；**不是**当前迭代必须立刻开工的任务书。  
> 关联：  
> - 功能现状：`../Grok桌面客户端 功能大纲.md`（2026-07-26 代码对照版）  
> - 本仓库桌面：`crates/jike-grok-desktop`、`crates/grok-session`  
> - 外部分析用 clone（可选，与产品无关）：`D:\grokbuild\pi-analysis`、`D:\grokbuild\openworker-analysis`

---

## 1. 我们的定位（对照前提）

| 维度 | 我们（Grok Build 桌面二次开发） |
|------|--------------------------------|
| 目标 | 把 xAI 开源 **Grok Build** 做成深度集成的桌面 coding agent（非套壳子进程） |
| 架构 | React/Tauri GUI → `GrokSession` → ACP + `tokio::io::duplex` → `xai_grok_shell` 库内嵌 |
| 原则 | **可改官方**（好方案时，见 `docs/官方代码修改原则.md`）；先查官方能力再自建；独立 `GROK_HOME` |
| 当前阶段 | 个人/小团队自用，Windows 主验证，主路径「选工作区 → 对话 → 工具/权限 → 恢复会话」 |

**不要用外部项目替换引擎**；只借鉴产品交互、安全与扩展面的做法，优先 **把官方 crate 已有能力接到 GUI**。

---

## 2. 外部项目一览

### 2.1 Pi（[earendil-works/pi](https://github.com/earendil-works/pi)）

| 项 | 内容 |
|----|------|
| 定位 | 极简、可扩展的 **终端 coding agent harness** |
| 技术 | TypeScript monorepo：`pi-ai` / `pi-agent-core` / `pi-coding-agent` / `pi-tui` |
| 入口 | CLI `pi`、Print/JSON、RPC、SDK |
| 扩展 | Extensions、Skills、Prompt templates、Themes、Pi packages |
| 安全观 | **无内置权限系统**；隔离靠 Docker / Gondolin / OpenShell |
| 官网 | https://pi.dev |
| 与我们关系 | **同类问题域**（coding agent），形态不同（终端 vs 我们桌面） |

### 2.2 OpenWorker（[andrewyng/openworker](https://github.com/andrewyng/openworker)）

| 项 | 内容 |
|----|------|
| 定位 | 本地优先 **桌面 AI 同事**（交付文档/日历/Slack 等，不单是写代码） |
| 技术 | Python agent server（aisuite）+ React/Tauri GUI + 连接器/MCP |
| 安全观 | 写操作/发送/shell **审批门控**；密钥本机 secret store |
| 与我们关系 | **形态接近**（Tauri 桌面壳），**产品方向不同**（知识工作连接器 vs coding agent） |

### 2.3 三者粗对照

| 维度 | 我们 | Pi | OpenWorker |
|------|------|-----|------------|
| 主场景 | 桌面写代码 / 改仓库 | 终端写代码 | 桌面日常任务 + SaaS 工具 |
| Agent 引擎 | 官方 `xai-grok-shell`（Rust） | `pi-agent-core`（TS） | aisuite + Python TurnEngine |
| 权限 | ACP 权限弹窗（已有） | 默认等同用户权限 | 强审批 + Inbox |
| 扩展 | MCP/插件官方有，GUI 弱 | Extensions/Skills 一等公民 | 25+ 连接器 + MCP |
| 密钥 | `.env` 明文 | 环境变量 / OAuth 登录 | 本机 secret store |
| 会话 | 恢复/列表/侧栏分组 | branch、compaction、多 mode | 会话 + 自动化 + unattended |

---

## 3. 可参考点（学什么 / 不学什么）

### 3.1 建议参考（实现时当设计灵感，不抄栈）

| 来源 | 可参考点 | 映射到我们 | 备注 |
|------|----------|------------|------|
| OpenWorker | 密钥进系统密钥环 / 不回显明文 | `$GROK_HOME/.env` → Windows Credential Manager 等 | **优先参考**；我们已有泄露风险 |
| OpenWorker | 本地 sidecar 的 Origin / token 意识 | 若将来暴露 HTTP/WS 再学 | 当前仅 Tauri invoke，风险较低 |
| OpenWorker | 审批 / Always-allow 的产品表达 | 权限弹窗、standing 规则（若引擎支持） | 我们已有权限弹窗，可打磨文案与「本会话总是允许」 |
| OpenWorker | GUI e2e / 打包节奏 | 发版前再考虑 | 自用阶段非必须 |
| Pi | Session 分支 / compaction **产品语义** | 先查官方 fork/compact API 再接 GUI | 不要重写会话存储 |
| Pi | Skills / Extensions 的「可发现 + 开关」 | MCP 列表、插件 enable、agent 列表 | 不做 TS 扩展运行时 |
| Pi | 工具结果分层展示 | 工具卡 diff 高亮、长输出可读 | 我们已有卡片 + preview 增量 |
| Pi | multi-surface 时状态机干净 | `GrokSession` 保持无 UI；多标签时多实例 | 多会话并行的前置条件 |
| 两者 | 长会话：分页/虚拟列表、搜索索引 | 消息全量 state、标题-only 搜索 | 官方可能有 search sqlite |

### 3.2 明确不参考 / 不做的方向

| 来源 | 不学什么 | 原因 |
|------|----------|------|
| Pi | 整仓换 TS / 换 `pi-ai` | 引擎在官方 Rust crate |
| Pi | 默认无权限 + 全靠 Docker | 我们已有 ACP 权限，应强化而非拆掉 |
| Pi | 核心只留 4 工具 | 我们工具栈与 MCP 已更完整 |
| OpenWorker | 25+ SaaS 连接器中台 | 偏离 coding desktop 主线 |
| OpenWorker | 再造 Python agent 循环 | 已有 shell 库内嵌 |
| 两者 | 为对标而堆功能（深色主题、i18n、商店…） | 当前阶段 ROI 低，见 §5 |

---

## 4. 我们现状速览（代码对照后，2026-07-26）

详见 `Grok桌面客户端 功能大纲.md`。摘要：

**已具备：** 库内嵌 ACP、流式文本/思考/工具、权限、取消、恢复/新建/删/重命名、空会话清理、模型配置与热切换、CWD 持久化、跨工作区侧栏、token 用量、工具卡 + preview 增量、设置页等。

**唯一「明确不做」：** `system_prompt_label` 自由编辑 GUI（自动生成即可）。

**其余缺口均属「可选排期」，不是文档禁止。**

---

## 5. 未做项：需不需要做（决策表）

判断口径（当前阶段）：

- **建议做**：安全 / 长会话可用性 / coding 读结果 / 接上引擎已有能力  
- **可做（有触发再上）**：有明确痛点或用户场景再做  
- **暂不需要**：对当前自用阶段几乎无收益，或成本远大于价值  

### 5.1 建议做（后续优先排期）

| ID | 功能 | 现状 | 参考 | 实现时注意 |
|----|------|------|------|------------|
| T1 | 密钥 → 系统密钥链 | `.env` 明文 | OpenWorker secret store | Windows Credential Manager；设置页仍只显示 is_set |
| T2 | 长会话性能（虚拟列表或历史分页） | 消息一次进 React state | 通用桌面最佳实践；Pi 长 transcript 体验 | 先查官方历史加载 API，避免自造 JSONL |
| T3 | 全文搜索（接 `session_search.sqlite`） | 仅标题过滤 | 大纲已写；官方索引优先 | 侧栏搜索弹层换数据源 |
| T4 | 思考过程折叠 | 有气泡无折叠 | 小改 | 默认折叠 / 可展开 |
| T5 | 工具 diff 语法高亮 | 纯文本 preview | Pi/Claude Code 工具卡 | 接 ACP Diff 内容，别只靠字符串猜 |
| T6 | MCP 配置 GUI（增删/启停） | 引擎有、GUI 无 | Pi Skills「可发现+开关」 | 先查官方 MCP 配置读写，只做管理面 |

### 5.2 可做（有触发再上）

| ID | 功能 | 触发条件（例） | 参考 |
|----|------|----------------|------|
| O1 | 多会话并行 | 等生成时经常要另开一窗干活 | Pi multi-surface / 多实例 Session |
| O2 | 会话 Fork/分支 | 常从某条消息试两条路线 | Pi branching；先查官方 API |
| O3 | 附件/图片输入 | 频繁贴截图报 bug、看设计稿 | TUI `Alt+V`；官方附件能力 |
| O4 | 流式终端专用视图 | 长命令刷屏、preview 不够用 | 现有 `tool_call_update` 可先加长/复制 |
| O5 | 快捷键体系（Cmd/Ctrl+N、+K 等） | 键盘重度用户抱怨 | 小改即可先补 2～3 个 |
| O6 | 会话导出 Markdown | 要分享/归档对话 | OpenWorker 交付物思路（轻量） |
| O7 | Agent 角色切换 GUI | 真有多 agent（plan/code/…） | 官方 agent_name / personas |
| O8 | E2E 集成测试 | 发版频繁、回归成本高 | OpenWorker Playwright；Pi faux provider |
| O9 | 跨平台验证 | 有人要 macOS/Linux | 官方本就跨平台，桌面壳需验 |
| O10 | 配置导入/导出 | 频繁换机、给同事拷配置 | 可先文档说明拷贝 `GROK_HOME` |

### 5.3 暂不需要（当前阶段）

| ID | 功能 | 原因 |
|----|------|------|
| N1 | 流式断线续传/重连 | 进程内 duplex，不是网络会话；挂了 `load_session` 即可 |
| N2 | 深色主题 | 纯皮肤，不挡主路径 |
| N3 | 系统通知 | 生成时多半盯着窗口 |
| N4 | i18n | 中文自用足够 |
| N5 | 无障碍体系化 | 外发/合规前再做 |
| N6 | 崩溃远程上报 | 本地 log 够自查 |
| N7 | 自动更新 / 签名安装包 | 自己编译自用 |
| N8 | 团队共享配置 | 非多人产品 |
| N9 | 插件市场 GUI | 配置/CLI 能装即可；商店是运营级 |
| N10 | OpenWorker 式 SaaS 连接器中台 | 产品方向不符 |
| N11 | 用 Pi/OpenWorker 替换引擎 | 架构原则禁止 |

---

## 6. 建议落地顺序（仅供后续，非当前强制）

```text
Phase A  安全与底座
  T1  密钥链
  T2  长会话性能（出现卡顿时抬到最前）

Phase B  找得到、看得清
  T3  全文搜索
  T4  思考折叠
  T5  工具 diff 高亮

Phase C  扩展面
  T6  MCP 配置 GUI

Phase D  有触发再开
  O1～O10 按真实痛点选
```

每做一项：改代码 → 手测主路径 → 更新 `Grok桌面客户端 功能大纲.md` 状态 → 可在本文 §7 记一行完成记录。

---

## 7. 完成记录（后续追加）

| 日期 | ID | 摘要 | 相关 commit/PR |
|------|-----|------|----------------|
| （空） | | | |

---

## 8. 外部仓库本地路径与文档链接

| 资源 | 路径或链接 |
|------|------------|
| 功能大纲（现状） | `../Grok桌面客户端 功能大纲.md` |
| Pi 源码（若保留 clone） | `D:\grokbuild\pi-analysis` |
| OpenWorker 源码（若保留 clone） | `D:\grokbuild\openworker-analysis` |
| Pi 仓库 | https://github.com/earendil-works/pi |
| OpenWorker 仓库 | https://github.com/andrewyng/openworker |
| Pi 文档 | https://pi.dev/docs/latest |
| OpenWorker 站点 | https://openworker.com |

> 外部 clone 与 monorepo **无代码依赖**，仅供阅读；不需要时可整目录删除，不影响构建。

---

## 9. 修订说明

| 日期 | 说明 |
|------|------|
| 2026-07-26 | 初版：合并 Pi/OpenWorker 对照、可参考/不参考、与大纲对齐的「建议做/可做/暂不需要」及后续 Phase |
